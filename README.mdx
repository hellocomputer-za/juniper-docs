# Juniper UI Documentation

**Documentation Version**: 2.0.0  
**Last Updated**: 2024-12-19  
**Framework**: ApostropheCMS 4.x, Bootstrap 5.3.x, Juniper UI 2.x  
**License**: MIT

## Description

This repository contains comprehensive documentation for the Juniper UI design system and component library. Juniper UI is built for ApostropheCMS 4.x applications and provides a robust foundation of reusable components, utilities, and patterns for rapid development of modern, accessible, and responsive web applications.

## Documentation Structure

```
juniper-docs/
├── README.mdx                    # Main documentation overview
├── components/                   # Component documentation
│   ├── accordion/                # Accordion widget docs
│   ├── button/                   # Button component docs
│   ├── headings/                 # Headings component docs
│   ├── lists/                    # Lists component docs
│   └── header-slider/            # Header slider widget docs
├── widgets/                      # Widget documentation
│   ├── accordion/                # Accordion widget docs
│   ├── button/                   # Button widget docs
│   └── header-slider/            # Header slider widget docs
└── elements/                     # Element documentation
    ├── headings/                 # Headings element docs
    └── lists/                    # Lists element docs
```

## Version Information for Juniper project

### Core Framework Versions
- **ApostropheCMS**: 4.x
- **Bootstrap**: 5.3.2
- **Swiper.js**: 11.0.0
- **AOS (Animate On Scroll)**: 2.3.1
- **Bootstrap Icons**: 1.11.2
- **Sass**: 1.69.0
- **Node.js**: 18.x+

### Component Library Versions
- **Juniper UI Core**: 2.0.0
- **Juniper Components**: 2.0.0
- **Juniper Templates**: 2.0.0
- **Juniper Utilities**: 2.0.0

### Build Tools
- **Vite**: 5.0.0
- **Sass Embedded**: 1.69.0
- **PostCSS**: 8.4.0

## Dependencies

### Required Libraries and Packages

#### Core Dependencies
```json
{
  "dependencies": {
    "apostrophe": "^4.0.0",
    "bootstrap": "^5.3.2",
    "swiper": "^11.0.0",
    "aos": "^2.3.1",
    "bootstrap-icons": "^1.11.2"
  },
  "devDependencies": {
    "@apostrophecms/vite": "^4.0.0",
    "sass-embedded": "^1.69.0",
    "vite": "^5.0.0",
    "postcss": "^8.4.0",
    "autoprefixer": "^10.4.0"
  }
}
```

### Advanced Configurations

#### 1. Custom Theme Configuration
```scss
// _custom-theme.scss
$juniper-primary: #007bff;
$juniper-secondary: #6c757d;
$juniper-success: #28a745;
$juniper-danger: #dc3545;
$juniper-warning: #ffc107;
$juniper-info: #17a2b8;

// Import Juniper UI with custom variables
@import "juniper-ui";
```

#### 2. Component-Specific Configuration
```javascript
// Custom component configuration
const juniperConfig = {
    accordion: {
        animation: 'slide',
        duration: 300,
        easing: 'ease-out'
    },
    slider: {
        autoplay: true,
        delay: 5000,
        effect: 'fade'
    },
    animations: {
        enabled: true,
        duration: 800,
        offset: 100
    }
};

// Initialize with custom config
JuniperUI.init(juniperConfig);
```

#### 3. Responsive Configuration
```scss
// Responsive breakpoints
$juniper-breakpoints: (
    xs: 0,
    sm: 576px,
    md: 768px,
    lg: 992px,
    xl: 1200px,
    xxl: 1400px
);

// Custom responsive utilities
@include media-breakpoint-up(md) {
    .custom-component {
        font-size: 1.25rem;
    }
}
```

## Performance Tips

### 1. Asset Optimization
```bash
# Optimize CSS
npm run build:css -- --minify

# Optimize JavaScript
npm run build:js -- --minify

# Optimize images
npm run optimize:images
```

### 2. Lazy Loading
```javascript
// Enable lazy loading for components
JuniperUI.init({
    lazyLoad: {
        components: true,
        images: true,
        threshold: 0.1
    }
});
```

### 3. Bundle Optimization
```javascript
// Tree shaking for unused components
import { Button, Accordion, Slider } from 'juniper-ui';

// Only import what you need
const components = [Button, Accordion];
JuniperUI.init({ components });
```

### 4. Caching Strategies
```html
<!-- Set proper cache headers -->
<meta http-equiv="Cache-Control" content="max-age=31536000">
<link rel="preload" href="/dist/css/juniper-ui.min.css" as="style">
<link rel="preload" href="/dist/js/juniper-ui.min.js" as="script">
```

### 5. Critical CSS
```html
<!-- Inline critical CSS -->
<style>
    /* Critical Juniper UI styles */
    .header-slider-widget { height: 100vh; }
    .accordion-widget { margin: 1rem 0; }
    .button-widget { display: flex; gap: 1rem; }
</style>
```

### 6. Image Optimization
```html
<!-- Use responsive images -->
<img src="image.jpg" 
     srcset="image-300w.jpg 300w, image-600w.jpg 600w, image-900w.jpg 900w"
     sizes="(max-width: 768px) 100vw, 50vw"
     alt="Description">
```

### 7. JavaScript Performance
```javascript
// Debounce event handlers
const debouncedHandler = JuniperUI.utils.debounce(function() {
    // Handle event
}, 250);

// Use requestAnimationFrame for animations
JuniperUI.utils.raf(function() {
    // Animation code
});
```

### 8. Memory Management
```javascript
// Cleanup on page unload
window.addEventListener('beforeunload', function() {
    JuniperUI.destroy();
});

// Cleanup specific components
JuniperUI.destroyComponent('slider');
```

### 9. CDN Optimization
```html
<!-- Use CDN with fallback -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script>
    if (typeof Bootstrap === 'undefined') {
        document.write('<script src="/local/bootstrap.min.js"><\/script>');
    }
</script>
```

### 10. Monitoring and Analytics
```javascript
// Performance monitoring
JuniperUI.monitor({
    metrics: ['loadTime', 'renderTime', 'interactionTime'],
    report: function(data) {
        // Send to analytics
        analytics.track('juniper_performance', data);
    }
});
```

## Browser Support

### Supported Browsers
- **Chrome**: 90+ ✅
- **Firefox**: 88+ ✅
- **Safari**: 14+ ✅
- **Edge**: 90+ ✅
- **Internet Explorer**: 11 (with polyfills) ⚠️

### Polyfills for IE11
```html
<!-- IE11 Polyfills -->
<script src="https://polyfill.io/v3/polyfill.min.js?features=es6,es2017,es2018"></script>
<script src="https://cdn.jsdelivr.net/npm/promise-polyfill@8/dist/polyfill.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/intersection-observer@0.12.0/intersection-observer.js"></script>
```

## Contributing

### Documentation Setup
```bash
# Fork and clone the documentation repository
git clone https://github.com/your-username/juniper-docs.git
cd juniper-docs

# Create a new branch for your changes
git checkout -b feature/update-documentation

# Make your changes to the MDX files
# Add new component documentation
# Update existing examples

# Commit and push your changes
git add .
git commit -m "Add documentation for new component"
git push origin feature/update-documentation
```

### Documentation Standards
- Follow the existing MDX structure and format
- Include version information and dependencies
- Provide comprehensive usage examples
- Add troubleshooting sections for complex components
- Update the main README when adding new components
- Use clear, descriptive commit messages

## License

This project is licensed under the MIT License - see the [LICENSE](LICENSE) file for details.

